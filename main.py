from omegaconf import OmegaConf

def main(cfgs):
    # 1st we initialize the dataset

    # 2nd we load initialize the tokenizer and pretrained model

    # 3rd we set up the required tool

    # 4th we process the dataset and get the DataLoaders

    # 5th we initialize the training process

    # 6th we start the training loop

    # 7th we evaluate the model

    # 8th we save the final model

